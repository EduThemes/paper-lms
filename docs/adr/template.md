# NNNN. Short imperative title

## Status

Proposed | Accepted | Deprecated | Superseded by [NNNN](./NNNN-...md)

## Context

What problem are we solving? What did the codebase look like before this
decision? What forces (technical, organizational, legal) constrain the
choice?

Keep this section narrative — a new contributor should be able to
understand the trade-off without reading the rest of the codebase.

## Decision

State the decision in declarative voice. Be specific. Name the file
paths, the type names, the env vars.

> **The convergence point is `X.Execute` in `internal/.../x.go`.**

## Consequences

- What's now easier?
- What's now harder?
- What invariants must other code uphold so this decision keeps working?
- What's explicitly out of scope?

Reference the verification artifact (a test file, an audit, a CI gate)
that catches violations.

## References

- `path/to/file.go` — where this decision lives in code
- `docs/audits/...md` — related audit or design memo
- `CHANGELOG.md` — relevant `### Phase ...` section
